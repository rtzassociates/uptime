<div id="subscription_service_<%= service_id %>">
  <%= form_for current_user.subscriptions.find_by_service_id(service_id),
      :html => { :method => :delete,  :class => 'inline' }, :remote => true do |f| %>
    <%= f.submit "Unsubscribe", :class => "btn btn-danger" %>
  <% end %>
</div>