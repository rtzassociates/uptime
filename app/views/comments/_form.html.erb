<%= form_for @comment do |f| %>          
  <div class="field">
    <%= f.text_area :content, :placeholder => "Comment on this event", 
          :class => "span6", :rows => 4 %>
  </div>
  <div class="actions">
    <%= f.submit "Submit", :class => 'btn' %>
  </div>
<% end %>
