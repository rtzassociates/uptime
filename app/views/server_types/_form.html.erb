<%= error_messages_for @server_type %>
<%= form_for @server_type, :html => { :class => "well" } do |f| %>
  <div class="field">
    <%= f.label :value, "Server Type" %>
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit :class => "btn" %>
  </div>
<% end %>
