<%= form_for @task_note do |f| %>
  <p>
    <%= f.text_area :content %>
  </p>
  <p>
    <%= f.submit "Submit", :class => 'btn btn-success' %>
    <%= link_to "Cancel", :back, :class => 'btn' %>
  </p>
<% end %>