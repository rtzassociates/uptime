<!DOCTYPE html>
<html>
<head>
  <title>Uptime</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <link href='http://fonts.googleapis.com/css?family=Fredoka+One' rel='stylesheet' type='text/css'>
  <%= javascript_include_tag "application" %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container">   
    <div class="row">
      <div class="span12">
        <div id="header">
          <%= link_to 'uptime', root_path, :class => 'logo' %>
        </div>
      </div>
      <%= yield %>
      <div class="span12">
        <div id="nav">
          <%= link_to "View Past Events", events_path, :class => 'nav' %>
          <%= link_to "Applications and Services", services_path, :class => 'nav' %>
          <%= link_to "User Management", users_path, :class => 'nav' %>
          <%= link_to "Status Types", statuses_path, :class => 'nav' %>
        </div>
        <div id="session">
          <% if logged_in? %>
            Signed in as <%= link_to current_user.username, current_user %>. <%= link_to "Sign out", signout_path %>
          <% else %>
            <%= link_to "Sign in", new_session_path %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
