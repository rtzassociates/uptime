<%= error_messages_for @site_environment %>

<%= form_for @site_environment, :html => { :class => "well" } do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit :class => "btn" %>
  </div>
<% end %>
